{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pybricks-upload",
            "type": "shell",
            "command": "${config:python.defaultInterpreterPath}",
            "args": [
                "-m",
                "pybricksdev",
                "run",
                "ble",
                "${file}"
            ]
        }
    ]
}